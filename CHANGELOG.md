# 更新日志

## [0.2.62] - 2026-09-26

### 新增

- 新增“单窗口看图”，可从设置或右键菜单开启并记住选择。后续打开的图片复用已有窗口，跨文件夹打开时同步切换左右翻图列表；一次打开多张图片时显示第一张。
- 新增全局“窗口置顶”，设置与右键菜单同步并在本地记忆，所有已打开和新建的看图窗口均生效；切换应用后保持在普通窗口上方，不抢占键盘焦点。

### 改进

- 单窗口换图保留窗口大小、位置和全屏状态，重置缩放、平移、旋转、OCR、截图及播放状态；重复打开当前图片仅激活窗口，加载失败保留原图，截图编辑中换图先确认。
- 原生全屏期间暂时取消置顶，退出或进入失败后按当前设置恢复；设置窗口与操作弹窗显示在置顶图片上方。

## [0.2.61] - 2026-09-26

### 新增

- 看图时按 `F` 或 `Control + Command + F` 进入／退出 macOS 原生全屏，幻灯片播放时同样可用。
- 全屏快捷键忽略长按重复触发，并在切换期间保持幻灯片播放／暂停状态。

### 改进

- 增大幻灯片播放间隔的字号，提升可读性。

## [0.2.60] - 2026-09-25

### 新增

- 新增幻灯片播放：从当前图片开始，按当前文件夹顺序自动播放；默认每张 5 秒并开启循环，支持 2、3、5、10 秒间隔，可手动关闭循环。
- 播放控制条支持上一张、暂停／继续、下一张、全屏切换和退出幻灯片；空格暂停／继续，Esc 返回普通看图。
- 播放期间空闲时自动隐藏控制条与信息浮层；缩放、截图、删除确认及切换应用等操作会暂停播放。

### 改进

- 统一看图与播放工具栏的高度、线框图标、选中态和分组间距，优化时间菜单及退出图标。
- 循环选中效果与截图标注工具保持一致，更新应用预览图中的播放入口。
- 棋盘格按图片透明度显示：普通照片及完全不透明的 PNG 不再显示，透明度判断结果会缓存并供主图、切图过渡与缩略导航图复用。
- Finder 排序读取失败时静默回退到名称顺序，不再显示失败提示。

### 修复

- 保持全屏切换前的播放／暂停状态，避免原生全屏过渡导致幻灯片意外结束。
- 自动播放跳过已移走或无法解码的图片；关闭循环后播放到末尾结束并保留当前图片。

## [0.2.59] - 2026-09-24

### 新增

- 统一设置窗口，提供“打开方式”“显示设置”“关于”三个页面；右键菜单入口直接定位对应页面，显示偏好与右键菜单及其他看图窗口同步。
- 支持直接按 Delete / Backspace 或向前删除键将图片移到废纸篓，保留原有 `⌘⌫`；长按快捷键不会连续删除多张图片。
- 手动检查更新时，如果已经是最新版本，显示提示；启动时的自动检查保持安静。

### 改进

- 普通 Finder 列表视图改为读取排序字段和方向后在本地排序，减少逐个查询文件导致的超时；分栏视图支持读取 Finder 保存的排列规则。
- 排序读取完成前的翻页操作会排队，完成后按读取到的顺序处理；读取失败时按名称升序并明确提示。仍需 Finder 自动化权限，部分视图与分组组合暂不支持。
- 删除提示采用更清晰的深浅色背景、中性废纸篓图标和紧凑布局；“撤销”按钮使用与“设为默认”一致的系统原生配色。
- 优化设置窗口居中、格式列表间距、浅色卡片边框和关于页排版，版本号隐藏 build 号，检查更新时保持布局稳定。
- 整理右键菜单的设置、更新和关于入口，并调整 DMG 安装窗口的位置。

## [0.2.58] - 2026-09-23

### 修复

- 修复点击 Finder 自动化授权弹窗后，看图窗口退到其他窗口后方的问题；首次授权选择“允许”或“不允许”后，恢复原看图窗口的焦点。
- 已有授权决定时不重复触发焦点恢复；等待期间关闭、最小化窗口、切换到其他桌面或隐藏应用时，跳过窗口恢复。

## [0.2.57] - 2026-09-23

### 改进

- 图片翻页支持双向循环：最后一张继续向后回到第一张，第一张继续向前回到最后一张；自动跳过已删除的图片，单张图片不循环。
- DMG 安装界面改为纯白背景，放大安装窗口时背景颜色保持一致。

### 修复

- 修复 Finder 自动化授权弹窗受排序查询的一秒超时影响、来不及点击就消失的问题。授权等待与排序查询分开处理，等待期间仍可按文件名顺序翻页。
- 修正 DDS 默认打开方式的格式标识，补齐 AVIF、ICO、TGA、DDS、OpenEXR、HDR、JPEG XL 的应用类型声明。
- 批量设置默认打开方式时跳过已设置的格式，单个格式失败后继续处理其他格式；失败提示显示具体格式和错误信息，并刷新实际设置状态。

## [0.2.56] - 2026-09-19

### 新增

- 透明图片默认显示 Photoshop 风格的灰白棋盘格，方便辨认透明区域与半透明边缘；棋盘仅用于预览，不会写入复制或导出的图片。
- 看图窗口支持 `⌘⇧A` 快速调出截图与标注，隐藏工具栏时也可使用；重复触发不会清空已有选区或标注。

### 改进

- 棋盘格在缩放过程中保持固定大小，随图片边界裁剪，并与旋转、平移和切图效果同步。
- 优化连续放大时的绘制开销，改善快速连点放大的流畅度，同时保留原有缩小动画的速度和缓动效果。
- 优化官网布局、搜索展示、分享预览与图标缓存，改善不同屏幕尺寸下的阅读体验。

### 修复

- 解决透明背景在非整数缩放比例下出现深色格线、格子被拉伸的问题。

## [0.2.55] - 2026-09-13

### 新增

- 支持直接把查看中的图片拖到桌面、Finder 文件夹或支持文件拖放的应用中复制保存，无需按 Option。
- 本地图片优先使用原文件，保留原始文件名、扩展名和内容，包括 GIF 动画；临时来源及内存图片提供文件导出兜底。
- 通过符号链接打开图片时，拖出真实源文件，避免动图被转换为静态图片。

### 交互

- 窗口内拖动继续用于图片平移，只有明确越过整个窗口边界及防误触距离后，才启动系统文件拖拽。
- 保留无标题栏顶部拖动窗口、右下角及左上角调整窗口大小，以及文字选择、缩略导航图和截图编辑的原有拖动操作。
- 提供系统拖拽缩略图与复制反馈；取消拖放不修改原图，切换图片会取消尚未启动的导出候选。
- 临时导出文件使用独立目录避免覆盖，并在后续启动或导出时清理过期文件，跳过仍在使用的来源进程。

## [0.2.54] - 2026-09-11

### 改进

- 优化看图与截图编辑工具栏的高度、间距、图标比例及悬停反馈，编辑按钮采用圆角正方形。
- 精简截图编辑栏的颜色、粗细、字号控件，改善图标与文字的视觉协调性。
- 优化缩放、旋转和切图动画，连续操作时衔接更自然，并遵循系统“减少动态效果”设置。
- 官网下载入口使用固定的最新版 DMG 地址；每次发布同时提供带版本号和固定文件名的安装包。
- README 新增截图与标注界面预览；移除不再使用的旧图标素材，打包时从当前图标源文件生成。

### 修复

- 修复截图宽高输入框聚焦时数字跳动的问题，保持连续输入光标位置和撤销分组稳定。
- 修复切图后立即进入截图、从图片外拖入框选时的交互问题。
- 本地构建统一安装到 `/Applications/PicSee.app`，与 DMG 的安装位置一致。

## [0.2.53] - 2026-09-10

### 改进

- 删除确认框的取消按钮调整为 96 × 32 点，删除按钮为 120 × 32 点，按钮间距为 12 点，圆角为 7 点，文字与留白更协调。
- 亮色模式下，「移到废纸篓」按钮采用浅灰实底、暖红文字和无描边样式，让删除操作更清晰。

## [0.2.52] - 2026-09-10

### 改进

- 优化删除确认框按钮的可见高度和圆角，保留左右排列、默认取消和红色删除文字。
- 按钮文字按可见区域水平、垂直居中，改善视觉对齐。

### 修复

- 修正自定义按钮底色绘制越界、原生布局边距导致按钮放大和间距被挤占的问题。
- 焦点框与按钮底色使用相同边界，保留按下反馈和键盘操作。

## [0.2.51] - 2026-09-10

### 新增

- 支持通过右键「移到废纸篓」或 `⌘⌫` 删除当前图片，默认先确认，并可选择「以后不再询问」。
- 支持通过成功提示、右键「撤销移到废纸篓」或 `⌘Z` 逐次恢复本次窗口中删除的图片，恢复时不会覆盖同名文件。
- 删除后自动切到下一张，末尾回到上一张；没有可浏览图片时显示空文件夹页面，保留撤销和退出入口。

### 改进

- 删除确认框采用左右按钮布局，分行显示标题与文件名，精简说明和留白；长文件名中间省略，删除操作以红色文字标识。
- 回车和 Escape 默认取消删除，长按删除快捷键不会连续删图，截图标注期间不触发原图删除。
- 「关于 PicSee」窗口的网址去掉下划线和末尾斜杠，保留点击跳转。
- 默认截图文件名调整为 `PicSee_截图_年月日-时分秒-毫秒.png`，更新官网展示图片与文案。

## [0.2.50] - 2026-09-08

### 修复

- 恢复两种标题栏模式原有的初始窗口尺寸策略：按图片像素及屏幕倍率计算，初始最小内容尺寸为 800 × 600 点，并以屏幕可用空间为限。
- 恢复原有手动缩放、固定窗口恢复的最小尺寸约束及视图层 480 × 320 点的最小布局限制，仅保留无标题栏界面的扁平化外观。

## [0.2.49] - 2026-09-08

### 改进

- 无标题栏看图窗口采用更扁平的样式：关闭窗口阴影，去掉信息浮层、导航按钮和工具栏的装饰性描边与投影，保留半透明底色及按下反馈。
- 非固定、无标题栏窗口按图片逻辑显示尺寸打开，最小内容尺寸为 360 × 240 点，减少小图周围的灰色留白。
- 保留有标题栏模式的系统窗口控制、原有浮层样式和初始尺寸策略，以及固定窗口的大小与位置偏好。

### 修复

- 统一窗口与画布拖拽的最小尺寸约束，移除重复的视图最小尺寸限制，改善小窗口切换标题栏时的布局兼容性。

## [0.2.48] - 2026-09-08

### 改进

- 优化非固定窗口的首次打开尺寸：结合图片像素和屏幕倍率计算，初始最小内容尺寸为 800 × 600 点，大图按可用区域缩小并居中，预留菜单栏、Dock 和标题栏空间。
- 截图选框统一使用蓝色，并在选框上方实时显示像素宽高；靠近窗口边缘时尺寸提示保持可见，底部仍可编辑宽高。
- 截图保存选项调整为“按选择尺寸保存”和“按原图尺寸保存”，保留输出像素尺寸及当前显示比例。

## [0.2.47] - 2026-09-08

### 改进

- 截图保存改为两个单选项，直接展示“按选择像素保存”和“按原图像素保存”的输出尺寸，并在原图尺寸后显示当前显示比例。
- 截图默认文件名改为 `picsee-截图-年月日-时分秒-毫秒.png`。
- 更新截图标注功能的展示图片。

### 修复

- 框选尺寸计入 Retina 屏幕倍率，底部显示与复制、按选择像素保存的输出尺寸一致。
- 按原图像素保存直接使用原图选区尺寸，避免重复放大。
- 改善较小显示比例下截图文字和细线被额外平滑的问题，复制与保存共用渲染逻辑。
- 输入选区宽高时按显示像素换算，修复只聚焦后离开宽高输入框也可能改变选区的问题。
- 鼠标进入截图工具面板时恢复标准箭头，返回图片区域后恢复选择指针。
