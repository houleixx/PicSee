# 截图快捷键与放大回归检查

## 截图快捷键

看图窗口按 `⌘⇧A`，调用与工具栏截图按钮相同的截图／标注入口。工具栏隐藏时仍有效；已有截图编辑状态不会重复创建，保留选区和标注。

快捷键接入现有窗口本地键盘监听和画布响应路径。沿用已有的当前窗口、文本输入、sheet 和截图编辑状态保护；不是全系统截图快捷键。

测试覆盖精确修饰键组合、按键重复，以及实际 SwiftUI 看图窗口在隐藏工具栏时通过按键调出截图编辑器、再次触发后保持同一编辑文档。

## 缩放与影响检查

连续放大的主要性能问题来自此前把棋盘画在图片视图中，导致放大时重建巨大的 backing。现已恢复原生图片视图并把棋盘移到独立视口背景。缩小的速度和缓动参数保持原样，放大继续使用同一套动画。

性能对照和背景回归细节见 [透明棋盘格与连续缩放修复](2026-09-19-transparency-grid.md)。

## 最终验证

`swift test`：302 项 XCTest、7 项 Swift Testing（包含参数化用例）全部通过。覆盖连续缩放、旋转、快速切图、减少动态效果、截图编辑和导出等现有功能。

本轮完整回归中连续放大夹具耗时 1.688 秒，连续缩小 1.924 秒。与前述性能对照一致，未对缩小动画参数做改动。

已构建 arm64 / x86_64 通用应用并安装到 `/Applications/PicSee.app`。构建产物与安装目录逐文件一致，签名校验通过；可执行文件 SHA-256 为 `51bd126ecc085afb6d132b2c7ef56c581faea7c0207d5c57f0f8cf62f49a7e3c`。

打开 `/tmp/PicSee-transparent-preview.png` 后，通过应用截图确认：透明和半透明区域显示灰白棋盘，背景仅覆盖图片矩形，窗口留白不显示棋盘；截图按钮提示包含 `⌘⇧A`。动态连续点击使用上述 AppKit/SwiftUI 自动化测试验证。
