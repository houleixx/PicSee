# 单窗口与窗口置顶验证

发布日期：2026-09-26；版本：0.2.62。

## 自动验证

- `swift test`：326 项 XCTest 与 58 项 Swift Testing 测试通过，共 384 项。
- 新增 8 项单窗口测试：路由、接收者退出/PID 重用、偏好持久化与菜单、新会话重置、跨目录翻图、同图/加载失败保留、同目录刷新、延迟目录结果失效、窗口尺寸复用。
- 新增 5 项置顶测试：默认关闭/持久化/菜单同步，多个已有及新建窗口层级，不改变焦点/窗口尺寸/Spaces，全屏退出与进入失败恢复最新偏好，设置及附属弹窗层级。
- `bash Tests/build-dmg-tests.sh`：打包参数传递检查通过。

## 本次会话中的隔离应用实测

- 强制启动第二个进程打开另一文件夹中的图片，请求转交原窗口；原图 63% 缩放恢复为新图默认 50%，右方向键浏览新文件夹的下一张。
- 单窗口设置与右键菜单同步。
- 截图编辑中换图出现确认；取消保留原图与编辑，确认后换图并退出截图编辑。
- 设置页显示“窗口置顶”及全局作用说明；开启后退出并重新启动，设置仍为开启。
- 置顶图片存在时，设置窗口可正常显示。

## 复核与边界

独立代码审查、需求/覆盖审计和故障时序静态复核未发现发布阻断。未宣称完整路径覆盖率：实际跨进程退出竞态、真实系统全屏中连续换图、最小化恢复和所有 OCR 后端的状态清理未全部进行端到端故障注入测试；对应生命周期与失效保护已经静态核对。

两个功能均默认关闭。置顶是全局偏好，单窗口模式开启时不关闭已有窗口。工具栏不增加设置按钮。
